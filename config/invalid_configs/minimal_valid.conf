# Test file for minimal valid configuration
# This should pass validation with the absolute minimum required directives

server {
    listen 8080;
    host 127.0.0.1;
    server_name localhost;
    
    location / {
        allowed_methods GET;
    }
}